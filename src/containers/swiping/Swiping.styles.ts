import { makeStyles } from '@material-ui/core'

export default makeStyles({
  root: {
    display: 'grid',
    gap: '1em',
    gridTemplateRows: 'auto max-content',
    height: '100%',
    padding: '1em 0',
  },
})
