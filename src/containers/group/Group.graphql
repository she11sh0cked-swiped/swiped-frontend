query group($id: MongoID!) {
  group_findById(_id: $id) {
    _id
    name
    ownerId
    membersId
  }
}

query user {
  user_findMe {
    _id
  }
}

mutation joinGroup($id: MongoID!) {
  group_joinById(_id: $id) {
    recordId
  }
}
