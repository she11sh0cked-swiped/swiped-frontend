config:
  scalars:
    MongoID: string

generates:
  src/types/api.ts:
    plugins:
      - typescript
      - typescript-apollo-client-helpers

overwrite: true

schema: http://localhost:4000/graphql
