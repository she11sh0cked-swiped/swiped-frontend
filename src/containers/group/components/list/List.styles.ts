import { makeStyles } from '@material-ui/core'

export default makeStyles({
  card: {
    width: '100%',
  },
  rating: {
    bottom: 0,
    margin: '0.5em',
    position: 'absolute',
    right: 0,
    textAlign: 'right',
  },
})
